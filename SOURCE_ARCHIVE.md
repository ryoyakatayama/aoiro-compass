# 過年度資料

「過年度資料」に資料台帳JSONを登録すると、原本の名前・元の場所・読み取り文字から検索できます。事業所得と雑所得の表示を分け、共通資料は両方に表示します。原本はGoogle Driveのアクセス権で保護されます。

資料の登録前に件数を表示し、現在の台帳を置き換える場合は明示します。仕訳や申告済みの金額は変更しません。確認事項への回答と状態は端末に保存し、帳簿のDrive同期に含めます。別端末で同じ回答を同時変更した場合は既存の同期競合画面で選択できます。

台帳の形式は `src/domain/archive.ts` の `archiveSchema` で定義しています。原本URLはGoogle Drive / Google DocsのHTTPS URLのみを受け付けます。実際の資料・OCR文字・台帳JSON・帳簿は公開リポジトリに入れないでください。

過年度の帳簿JSONは「設定 → 過年度JSONを確認」から登録します。`book` に `business` または `misc` がある場合、所得区分が一致する帳簿でのみ取り込めます。既存年度は上書きせず、登録した年度は閲覧専用です。

集計のみの年度では、月別の `revenue` / `expense` を `null` にすると未集計として扱います。0円とは区別し、利益は両方が判明している月だけ表示します。年次合計と判明済みの月次合計の整合性も検査します。

家事按分や返金による月次の売上・経費のマイナスにも対応します。12か月分が判明した項目について年次合計と一致を検査し、未集計の月に勝手な補完をしません。

CSV読み込みでは、拡張子だけCSVになっているExcelファイルを検出し、Excelでの変換手順を案内します。
