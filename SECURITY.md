# セキュリティとデータの扱い

標準構成はGitHub Pagesでアプリを公開し、正本・バックアップを利用者のGoogle Driveへ保存します。アプリ独自の暗号化や共有のアプリパスワードは使いません。Google OAuthの認可が必要であり、公開Client IDだけでは他人のデータを読み出せません。

- リポジトリへ実帳簿・原本・トークン・秘密鍵を送るコードはありません。Client SecretやAI APIキーは不要です。
- OAuthアクセストークンはメモリ内のみです。Driveの同期先はGoogleアカウントとClient IDで固定し、違う接続先への同期を拒否します。
- 帳簿履歴・バックアップは共有できないappDataFolderを使用します。原本用フォルダでは初回確認時に共有済みのフォルダを拒否します。利用後に利用者がDrive側で共有設定を変更した場合まで防止するものではありません。
- 原本、変更履歴、バックアップは追加保存し、既存原本の上書き・削除APIは使いません。SHA-256とスキーマ・SQLiteの整合性を検証します。チェックサムは暗号化や第三者署名ではありません。
- 複数端末の同時変更は因果関係に基づいて合成し、同じ項目への変更・削除が重なれば明示的な確認を待ちます。タイムスタンプだけで消去・上書きしません。
- AI回答・原本・CSVは入力データとして扱います。AI候補の自動確定、JSONから任意SQLの実行、任意HTMLの挿入は行いません。
- 端末の作業用SQLite・原本キャッシュ・一時退避、明示的に書き出す相談ZIPやCSVは平文です。OSの保護と画面ロックを使ってください。Drive未送信分はクラウドから復旧できません。
- `noindex` は検索エンジンへの依頼です。GitHubの公開ソースやアプリ画面の閲覧制限とは異なります。
- 同一生成元の別サイト、悪意あるブラウザ拡張、端末自体の侵害、GitHubやGoogleアカウントの乗っ取りにはアプリだけで対抗できません。GoogleとGitHubのアカウント保護を有効にし、同じPages生成元へ信頼できないコードを置かないでください。

任意の認証サーバーはsalt付きscrypt、ランダムセッション、HttpOnly/Secure/SameSite Cookie、Origin検証、試行回数制限、CSP、パス検証を使用します。標準のPages配信では動作しません。

2026-09-12に検出したVitest開発依存の既知脆弱性（GHSA-82fw-gwwq-j7x9）は4.1.11へ更新して解消しました。更新後の `pnpm audit` は既知脆弱性0件です。これは独立した侵入試験や、脆弱性がないことの保証ではありません。

実際のOAuthクライアント・Google Driveを使った接続検証は未実施です。テスト用Drive APIで同期、バックアップ失敗、復旧を検証しています。実運用前に少数の試験データで接続・復旧を確認してください。

問題を報告する場合、公開Issueへ実帳簿・領収書・認証情報を添付しないでください。個人情報を含まない再現手順を使用してください。
