# GitHub Pagesで使う

青色コンパスはGitHub Pagesだけで配信できます。新しいサーバー契約や、自宅PCの常時起動は不要です。

| 保存するもの                       | 保存先                             | 保護方法                                 |
| ---------------------------------- | ---------------------------------- | ---------------------------------------- |
| アプリ本体・アイコン               | GitHub / GitHub Pages              | 公開。会計データは含めない               |
| Google OAuth Client ID             | GitHubの設定、またはアプリ設定     | 公開可能な識別子。秘密鍵ではない         |
| 正式な帳簿・変更履歴・バックアップ | 自分のGoogle Driveのアプリ専用領域 | Googleアカウントの認証・アクセス権       |
| 領収書の原本                       | 自分のGoogle Driveの専用フォルダ   | フォルダの共有を制限                     |
| 操作用の帳簿・未送信の領収書       | 自分の端末のブラウザ               | OSのログイン・画面ロック・ディスク暗号化 |
| Googleアクセストークン             | 開いているアプリのメモリだけ       | 再起動・期限切れで再接続                 |

今回の構成では、GitHubに暗号化した秘密情報を置く必要もありません。Drive情報を知っているだけでは他人の帳簿にアクセスできず、Googleで本人の許可が必要です。Drive・端末の帳簿にはアプリ独自の暗号化をかけないため、独自パスワードの紛失で復旧できなくなる問題を避けます。

## 1. Googleの初期設定

1. [Google Cloud Console](https://console.cloud.google.com/)でプロジェクトを用意し、Google Drive APIを有効にします。
2. Google Auth Platformで同意画面を設定します。個人のテスト利用では自分をテストユーザーに登録します。
3. データアクセスに `drive.readonly`、`drive.file`、`drive.appdata` を設定します。全体の読み取りは、外部から追加した領収書の発見・読み込みに使います。
4. クライアントの種類を「ウェブアプリケーション」にします。承認済みJavaScript生成元に `https://ryoyakatayama.github.io` を登録します。`/aoiro-compass/` のパスは付けません。ローカル開発時は利用する `http://127.0.0.1:5173` 等も別に登録します。
5. 発行された **Client ID** を控えます。Client Secret、Googleのパスワード、APIキーはアプリやリポジトリに入力しません。

同じClient IDをPCとスマホで使用します。Googleのテストモード・機密性の高いスコープには利用制限や審査条件があります。実際にGoogleが表示する案内に従ってください。[スコープの公式説明](https://developers.google.com/workspace/drive/api/guides/api-specific-auth)

## 2. Pagesへ配信

1. GitHubのリポジトリで **Settings → Pages → Source → GitHub Actions** を選びます。
2. **Settings → Secrets and variables → Actions → Variables** に `VITE_GOOGLE_CLIENT_ID` を作り、上のClient IDを入力します。これは公開可能な値です。空欄でビルドした場合も、アプリ内で入力できます。
3. **Actions → Publish app to GitHub Pages (manual) → Run workflow** を実行します。
4. 完了後のアプリURLは `https://ryoyakatayama.github.io/aoiro-compass/` です。Actionsが失敗している間や初回の配信前はアクセスできません。
5. アプリを開いて「事業情報・設定」からGoogleへ接続します。保存先は空欄で構いません。アプリが自分専用のフォルダを作ります。
6. 最初は少数の試験データで、Drive保存済み表示・原本表示・復旧を確認してから本帳簿を使ってください。

公開コードにはサンプルデータとテストだけを入れます。実際のSQLite、領収書、相談ZIP、`.env.local`、パスワードは追加しません。`VITE_` で始まるビルド変数はブラウザに公開されます。

アプリには `noindex` を設定していますが、検索除外の保証やアクセス制限ではありません。GitHubの公開ソースは検索・閲覧できます。アプリ画面は誰でも開けますが、あなたのGoogle Driveの帳簿が公開されることはありません。他人が自分のデータでアプリを利用することまで禁止する場合は、後述の認証付き配信が別途必要です。

## 3. スマホ・日々の保存

- スマホで同じHTTPSのURLを開き「ホーム画面に追加」します。勘定科目の追加、仕訳編集、原本の取込先選択もできます。
- 新しい端末は「事業情報・設定 → Driveから環境を復旧」で既存帳簿を読み込みます。事業所得・雑所得をそれぞれ復旧してください。
- Googleに接続中は変更後約2秒と30秒間隔で同期します。同じ仕訳の競合は両方を保管し、採用する内容を選びます。
- 原本のアップロード、帳簿の同期、復元用バックアップの保存に成功してから「Drive保存済み」と表示します。バックアップ失敗も表示します。
- オフラインや未接続の編集は、端末だけに残る未送信データです。アプリを閉じる前に保存状態を確認してください。端末を消去すると未送信分は復旧できません。
- Googleの認証が期限切れになったとき、ページを再読込したとき、所得区分を切り替えたときは再接続が必要です。閉じたアプリから常時バックグラウンド同期する方式ではありません。[Googleトークンモデル](https://developers.google.com/identity/oauth2/web/guides/use-token-model)
- 年度締めと手動バックアップはDriveへの接続が必要です。オフラインの大きな変更前の退避は一時キューに置き、接続後にDriveへ送信します。

## 4. 帳簿を開けない・新しい端末で再現したい

1. 設定画面または起動エラー画面から「Driveから環境を復旧」を開きます。直接開く場合はアプリURLに `?recover=1` を付けます。
2. 事業所得・雑所得のどちらを戻すか選び、以前と同じClient ID・Googleアカウントで接続します。
3. Drive上の仕訳・領収書の件数を確認します。競合があれば採用する内容を選びます。
4. 他の帳簿タブを閉じ、「復旧」と入力して実行します。現在の端末ファイルがある場合は、置き換える前にDriveへ退避します。退避に失敗した場合は置き換えません。
5. 帳簿、科目、事業情報、相談履歴、Driveの保存先が戻ります。原本はDriveに残り、必要なときに読み込みます。Googleへ再接続して同期を再開します。

復旧画面は壊れたローカルSQLiteを開かずに動作します。独自の復号鍵は必要ありません。復旧できるのはDriveに届いた変更までです。Drive側で元データを削除した場合、Googleアカウントに入れない場合、元のOAuthプロジェクトを失った場合まで復元できるわけではありません。

日時を指定した過去の状態へ戻す場合は「Google Driveのバックアップ → 履歴を表示 → 復元内容を確認」を使います。原本は別ファイルで、SQLiteだけには含まれません。復元した変更は次の同期で他の端末にも伝わります。

帳簿履歴とSQLiteバックアップはDriveの **アプリ専用領域（appDataFolder）** にあり、通常のフォルダ一覧には表示されません。アプリ内から一覧・コピー・復元ができます。Googleの「アプリデータを削除」を実行するとこれらも失われます。[専用領域の公式説明](https://developers.google.com/workspace/drive/api/guides/appdata)

## 管理・保守

GitHub Pagesなら自分でサーバーのOSを管理する必要はありません。必要なのは、依存ライブラリの更新、CI結果の確認、Googleの接続設定・Drive容量の維持、バックアップから復旧できることの確認です。完全に保守不要ではありません。現版では履歴やバックアップを自動削除しません。

同じ `ryoyakatayama.github.io` にある別のアプリはブラウザ上で同じ生成元です。そのアプリに悪意あるコードがあればローカル保存にアクセスし得るため、同じ生成元には信頼できるアプリだけを置いてください。分離が必要なら青色コンパス専用の独自ドメインを使用します。

## 任意：画面自体もパスワードで制限する場合

これは標準のPages構成では使いません。Node.jsを動かせるホストが別途必要です。

```sh
pnpm build:private
pnpm password:hash
```

対話入力で得たハッシュをホストの秘密の環境変数 `AOIRO_PASSWORD_HASH` に設定し、`AOIRO_ORIGIN=https://実際のホスト名` を指定して `pnpm start` で起動します。Dockerfileも同梱しています。HTTPSを終端するホストで動かし、必要に応じて `HOST=0.0.0.0`、`PORT=8080` を指定します。

認証版はすべてのアセットをサーバーで保護し、認証迂回を避けるためService Workerのオフライン起動を無効化します。パスワードをJavaScriptへ埋め込みません。秘密値はDriveやGitHubへ配置せず、ホストの秘密設定で管理します。セッションは8時間で期限切れ、サーバー再起動でも解除されます。サーバーは未配置です。
